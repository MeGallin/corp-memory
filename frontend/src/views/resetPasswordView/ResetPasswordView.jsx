import './ResetPasswordView.scss';

const ResetPasswordView = () => {
  return (
    <div className="reset-password-wrapper">Reset password for to follow</div>
  );
};

export default ResetPasswordView;
